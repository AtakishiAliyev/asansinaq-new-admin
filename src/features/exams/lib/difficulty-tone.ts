// The one colour code for difficulty across the builder: the filter, the
// result rows, the slots and the mix bar all use it, so a glance at any of
// them reads the same way.
export const DIFFICULTY_TONE: Record<number, string> = {
  1: 'bg-emerald-500',
  2: 'bg-sky-500',
  3: 'bg-rose-500',
}
