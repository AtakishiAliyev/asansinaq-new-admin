# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.claude/settings.local.json
.claude/launch.json
# Local test material (commercial books, never committed). Deliberately NOT
# under public/: vite copies public/ into dist/ verbatim, so a book parked
# there rides along into every build and any static deploy publishes it.
local/

# Copied from pdfjs-dist on install (scripts/copy-pdfjs-wasm.mjs)
public/pdfjs/
